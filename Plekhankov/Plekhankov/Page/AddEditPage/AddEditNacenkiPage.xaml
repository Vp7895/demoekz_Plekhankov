<Window x:Class="Plekhankov.Page.AddEditPage.AddEditNacenkiPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Plekhankov.Page.AddEditPage"
        mc:Ignorable="d"
        Title="AddEditNacenkiPage" Height="450" Width="800" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Column="0" Grid.Row="0" Text="Тип наценки:" Style="{StaticResource txtBlock}"/>
        <TextBlock Grid.Row="1" Text="Значение: " Style="{StaticResource txtBlock}"/>

        <TextBox Grid.Column="1" Grid.Row="0" Style="{StaticResource txtBox}" Text="{Binding type}"/>
        <TextBox Grid.Column="1" Grid.Row="1" Style="{StaticResource txtBox}" Text="{Binding znachenie}"/>

        <Button Content="Сохранить" Grid.Row="2" Name="SaveBtn" Style="{StaticResource btn}" Grid.ColumnSpan="2" Click="SaveBtn_Click"/>

    </Grid>
</Window>
